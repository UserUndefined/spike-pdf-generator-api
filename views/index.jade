extends layout

block content
  h1= title
  p Welcome to #{title}

  input(type='button', value='Submit', onclick='generate()')
  script.
      function generate() {
          window.location.href = '/generate'
      }